package test;

public class Text implements Comparable<Text>
{

    private RectF defaultSize;

    public Text ()
    {
        defaultSize.set(
                defaultSize.left,
                defaultSize.top,
                defaultSize.right,
                defaultSize.bottom
        );
    }

}