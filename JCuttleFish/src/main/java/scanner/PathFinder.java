package scanner;

//This class resolves the path that was given as an argument, verifies it's a non-empty path
//and saves the canonical paths of all FILES in a collection.

public class PathFinder {

	
}
